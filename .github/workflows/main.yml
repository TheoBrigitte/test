on:
  push:
  pull_request:
    types: [opened, reopened]

jobs:
  manage_changelog:
    runs-on: ubuntu-latest
    name: Manage changelog
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Manage changelog
        uses: TheoBrigitte/test@c294275cd98c9d0b9dce894012abb73d7a9ccee3
