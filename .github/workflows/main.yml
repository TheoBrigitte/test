on:
  push:
  pull_request:
    types: [opened, reopened]

jobs:
  manage_changelog:
    runs-on: ubuntu-latest
    name: Manage changelog
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Manage changelog
        uses: TheoBrigitte/test@67c0aa9a95be718a84f4ee90f5e6be17694a5841
